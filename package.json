{
  "name": "mfe-shop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "shell": "pnpm --filter shell dev",
    "products": "pnpm --filter products dev",
    "cart": "pnpm --filter cart dev",
    "all": "concurrently \"pnpm --filter products dev\" \"pnpm --filter cart dev\" \"pnpm --filter shell dev\"",
    "all_with_preview": "concurrently \"pnpm --filter products preview\" \"pnpm --filter cart preview\" \"pnpm --filter shell dev\"",
    "lint": "eslint \"./packages/**/*.{js,ts,vue}\" --ext .js,.ts,.vue",
    "lint:fix": "eslint \"./packages/**/*.{js,ts,vue}\" --ext .js,.ts,.vue --fix",
    "format": "prettier --write \"./**/*.{js,ts,vue,json,md}\"",
    "prepare": "husky install",
    "deploy": "pnpm --filter products run build && pnpm --filter products exec firebase deploy && pnpm --filter cart run build && pnpm --filter cart exec firebase deploy && pnpm --filter shell run build && pnpm --filter shell exec firebase deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.19.0",
  "dependencies": {
    "@mdi/font": "^7.4.47",
    "firebase": "^12.4.0",
    "pinia": "^3.0.4",
    "vue": "^3.5.22",
    "vue-router": "^4.6.3",
    "vuetify": "^3.11.3"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "husky": "^9.1.7"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,vue}": [
      "eslint --fix",
      "prettier --write",
      "git add"
    ],
    "*.{md,json}": [
      "prettier --write",
      "git add"
    ]
  }
}
